# PUBLIC ENVIRONMENT VARIABLES (Create React App will expose REACT_APP_* to the client)
# Instructions:
# - Copy this file to .env in the same directory.
# - Replace values as needed. Do not commit secrets.
# - The orchestrator will set .env for deployments.

# Base URL for APIs (if you later connect to a backend). Keep trailing slash if required.
REACT_APP_API_BASE_URL=https://api.example.com/

# Site URL used for redirects in future integrations (e.g., Supabase or OAuth flows)
REACT_APP_SITE_URL=http://localhost:3000/

# Graphics quality preset: low | medium | high
REACT_APP_QUALITY_PRESET=medium
